import { ScorecardAdmin } from "@/components/admin/scorecard-admin";

export const metadata = { title: "Scorecard · Khazanay" };

export default function ScorecardPage() {
  return <ScorecardAdmin />;
}
